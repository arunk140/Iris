[project]
name = "watcher"
version = "0.1.0"
description = "Project Iris - Watcher: polls source directory and publishes video.new to RabbitMQ"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pika>=1.4.1",
]
