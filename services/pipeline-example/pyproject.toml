[project]
name = "pipeline-example"
version = "0.1.0"
description = "Project Iris - Example pipeline stage: consumes and logs messages (template for extending the pipeline)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pika>=1.4.1",
]
